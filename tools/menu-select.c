#include "menu-select.h"

/* 
 * user selects from a null terminated singly linked list of elements;
 * the index of the selected element is returned
 *
 */

public:
	int select_from_list(struct List elements){
		
	}
